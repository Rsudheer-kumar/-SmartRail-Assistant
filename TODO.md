- [ ] Modify addIRCTCButton() in frontend/script.js to use an <a> link instead of window.open (avoid popup blocking / click handler issues).
- [ ] (Optional) Add a small console log / fallback behavior to verify the click path.
- [ ] Run quick local test: trigger bot response, confirm IRCTC button appears and opens IRCTC.

