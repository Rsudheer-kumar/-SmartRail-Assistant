# TODO_DATE_FIX.md

- [x] Update frontend: include `source` and `destination` when submitting the selected travel date.
- [x] Update backend: read `source`/`destination` from request body and use it to build date-aware train list when session route is missing.
- [ ] Test locally: Delhi → Phagwara; choose two different dates; confirm train list appears for both.

